# How to install stuff first

- install latest matlab
- python 3.10 (mac = brew, linux = based on distro, windows = winget)
- python ensurepip --upgrade
- pip install [matplotlib, matlabengine (ensure matlab path is provided), xdasensapi (inside mtw sdk), numpy, socket]